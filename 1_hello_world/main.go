package main

import "fmt"

// package is multiple files in a logical grp -> main package has main function from where our, fmt = format package from standard library -> , which prints hello world
// package main is the entry point of the program
func main() {
	fmt.Println("Hello, World!")
}
